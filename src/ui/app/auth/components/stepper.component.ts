import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'c-auth-stepper',
  templateUrl: './stepper.component.html',
  imports: [
    // Modulos de angular
    CommonModule,

    // Modulos de librerias externas
  ],
})
export class CAuthStepper { }
