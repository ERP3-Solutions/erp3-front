import { Component } from '@angular/core';

@Component({
  selector: 's-finance-accounts',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './accounts.screen.html',
})
export class SFinanceAccounts { }