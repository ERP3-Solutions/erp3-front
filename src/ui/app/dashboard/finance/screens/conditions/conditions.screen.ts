import { Component } from '@angular/core';

@Component({
  selector: 's-finance-conditions',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './conditions.screen.html',
})
export class SFinanceConditions { }