import { Component } from '@angular/core';

@Component({
  selector: 's-finance-methods',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './methods.screen.html',
})
export class SFinanceMethods { }