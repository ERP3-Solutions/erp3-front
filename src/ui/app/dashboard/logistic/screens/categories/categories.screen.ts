import { Component } from '@angular/core';

@Component({
  selector: 's-logistic-categories',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './categories.screen.html',
})
export class SLogisticCategories { }