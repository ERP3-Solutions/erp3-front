import { Component } from '@angular/core';

@Component({
  selector: 's-logistic-list-prices',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './list-prices.screen.html',
})
export class SLogisticListPrices { }