import { Component } from '@angular/core';

@Component({
  selector: 's-logistic-products',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './products.screen.html',
})
export class SLogisticProducts { }