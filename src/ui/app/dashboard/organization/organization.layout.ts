import { Component } from '@angular/core';

@Component({
  selector: 'l-organization',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './organization.layout.html',
})
export class LOrganization { }