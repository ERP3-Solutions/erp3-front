import { Component } from '@angular/core';

@Component({
  selector: 's-organization-electronic-series',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './electronic-series.screen.html',
})
export class SOrganizationElectronicSeries { }