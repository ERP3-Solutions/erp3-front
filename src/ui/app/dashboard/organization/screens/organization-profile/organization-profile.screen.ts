import { Component } from '@angular/core';

@Component({
  selector: 's-organization-organization-profile',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './organization-profile.screen.html',
})
export class SOrganizationOrganizationProfile { }