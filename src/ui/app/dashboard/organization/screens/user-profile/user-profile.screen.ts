import { Component } from '@angular/core';

@Component({
  selector: 's-organization-user-profile',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './user-profile.screen.html',
})
export class SOrganizationUserProfile { }