import { Component } from '@angular/core';

@Component({
  selector: 's-sales-point-sales',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './point-sales.screen.html',
})
export class SSalesPointSales { }