import { Component } from '@angular/core';

@Component({
  selector: 's-sales-vouchers',
  imports: [
    // Modulos de angular
    // Modulos de librerías externas
    // Modulos de capas externas
    // Componentes(standalone) internos
    // Componentes(standalone) externos
  ],
  templateUrl: './vouchers.screen.html',
})
export class SSalesVouchers { }